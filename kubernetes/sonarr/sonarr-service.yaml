apiVersion: v1
kind: Service
metadata:
  name: sonarr-service
spec:
  type: NodePort
  selector:
    app: sonarr
  ports:
    - port: 8989
      targetPort: 8989
      nodePort: 30089 # Fester Port für Browser-Zugriff
