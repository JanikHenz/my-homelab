apiVersion: v1
kind: Service
metadata:
  name: piper-service
  namespace: jarvis
spec:
  type: ClusterIP
  selector:
    app: piper
  ports:
    - protocol: TCP
      port: 10200
      targetPort: 10200
