apiVersion: v1
kind: Service
metadata:
  name: wger-service
  namespace: fitness
spec:
  selector:
    app: wger-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30010
  type: NodePort
