apiVersion: v1
kind: Service
metadata:
  name: wger-db-service
  namespace: fitness
spec:
  selector:
    app: wger-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
