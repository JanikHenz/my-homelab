apiVersion: v1
kind: Service
metadata:
  name: flaresolverr-service
  namespace: media
spec:
  # ClusterIP only - internal service used by Prowlarr, no public access needed
  type: ClusterIP
  selector:
    app: flaresolverr
  ports:
    - name: http
      port: 8191
      targetPort: 8191
